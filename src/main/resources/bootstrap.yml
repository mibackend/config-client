spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://localhost:8888
      label: main
  profiles:
    active: prod
#    active: dev

server:
  port: 8887


management:
  endpoints:
    web:
      exposure:
        include: refresh

eureka:
  client:
    service-url:
      defaultZone=http://localhost:8761/eureka: